/*
describe('Testing authService Service', function () {
    var _Articles;
    beforeEach(function () {
        module('myApp');
        inject(function (authService) {
            _Articles = authService;
        });
    });
    it('Should be registered', function () {
        expect(_Articles).toBeDefined();
    });
    it('Should include logout loggedin checkLoggedin check methods', function () {
        expect(_Articles.checkLoggedin).toBeDefined();
        expect(_Articles.loggedin).toBeDefined();
        expect(_Articles.logout).toBeDefined();
    });
});
    */